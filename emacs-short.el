
(defun annai ()
  (interactive)
  (find-file (expand-file-name "~/workspace/tw/annai")))

(defun basilisk ()
  (interactive)
  (find-file (expand-file-name "~/workspace/tw/annai/basilisk")))

(defun buckbeak ()
  (interactive)
  (find-file (expand-file-name "~/workspace/tw/annai/buckbeak")))

(defun satyr ()
  (interactive)
  (find-file (expand-file-name "~/workspace/tw/annai/satyr")))

(defun thiasos ()
  (interactive)
  (find-file (expand-file-name "~/workspace/tw/annai/thiasos")))

(defun hippogriff ()
  (interactive)
  (find-file (expand-file-name "~/workspace/tw/annai/hippogriff")))


(provide 'emacs-short)
